apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: grafana
  namespace: grafana
spec:
  chart:
    spec:
      version: "7.0.17"
  test:
    enable: false
  # values:
  #   ingress:
  #     hosts:
  #       - host: podinfo.staging
  #         paths:
  #           - path: /
  #             pathType: ImplementationSpecific
